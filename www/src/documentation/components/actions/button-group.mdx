---
title: ButtonGroup
github: Button/ButtonGroup.tsx
---

import { Code, MessageBar } from '@looker/components'

## ButtonGroup

<MessageBar noActions intent="inform">
  <Code>ButtonGroup</Code> do not support uncontrolled versions.
</MessageBar>

`ButtonGroup` behaves like a checkbox group. Use this when the user may select zero or more of a small set of options.
Control externally via `value` and `onChange` props. On individual `ButtonItem`, `value` is optional and will fall back to the text if absent.

```js
;() => {
  const [value, setValue] = useState(['CA'])
  return (
    <>
      <ButtonGroup value={value} onChange={setValue}>
        <ButtonItem value="CA">California</ButtonItem>
        <ButtonItem value="AK">Alaska</ButtonItem>
        <ButtonItem value="UT">Utah</ButtonItem>
      </ButtonGroup>
      <Text pl="small">Current selection: {value.join(', ')}</Text>
    </>
  )
}
```

### Disabled

```jsx
<ButtonGroup value={['CA']} disabled>
  <ButtonItem value="CA">California</ButtonItem>
  <ButtonItem value="AK">Alaska</ButtonItem>
  <ButtonItem value="UT">Utah</ButtonItem>
</ButtonGroup>
```

## Options

An alternative to composing a `ButtonGroup` or `ButtonToggle` with `ButtonItem` children is to pass it an array of `options`.

```jsx
;() => {
  const options = [
    { value: 'Gouda', label: 'Smoked Gouda' },
    { value: 'Cheddar' },
    { value: 'Swiss', disabled: true },
  ]
  const [value, setValue] = useState([options[0].value])
  return <ButtonGroup options={options} value={value} onChange={setValue} />
}
```
