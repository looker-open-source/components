---
title: Spinner
propsOf: Spinner
github: Spinner
---

```jsx
<Spinner />
<Spinner size={80} markers={20} markerRadius={50} color="salmon" speed={2000} />
```

## SpinnerWrapper

Use `SpinnerWrapper` to align the `Spinner` in the middle of an element, with some padding.

```jsx
<SpinnerWrapper>
  <Spinner />
</SpinnerWrapper>
```
