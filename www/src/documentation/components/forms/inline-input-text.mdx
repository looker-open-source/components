---
title: InlineInputText
github: InlineInputText/InlineInputText.tsx
storybook: true
---

## InlineInputText

Input styled to have underline follow the input.

```jsx
<Flex alignItems="center" justifyContent="space-around">
  <Heading>
    <InlineInputText value="Type here..." />
  </Heading>
  <Paragraph variant="subdued">
    <InlineInputText placeholder="Type here..." />
  </Paragraph>
</Flex>
```

## Simple

Using the prop `simple` the input won't be as visible until hover or focus

```jsx
<InlineInputText simple value="Type here..." />
```

## Disabled

Using the prop `disabled` the input won't be editable

```jsx
<InlineInputText disabled value="can't change me" />
```

## ReadOnly

Using the prop `readOnly` the input won't be editable

```jsx
<InlineInputText readOnly value="can't change me" />
```

Using external value.

```jsx
;() => {
  const [value, setValue] = React.useState('Type here...')
  const onChange = (e) => {
    setValue(e.currentTarget.value)
  }
  const onClick = (e) => {
    setValue('You clicked the button')
  }
  return (
    <Flex alignItems="center" justifyContent="space-around">
      <InlineInputText value={value} onChange={onChange} />
      <Button onClick={onClick}>Click Me</Button>
    </Flex>
  )
}
```
