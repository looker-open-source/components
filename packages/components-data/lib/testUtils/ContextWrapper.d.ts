import type { FC } from 'react';
import type { DataStore } from '../hooks';
declare type ContextWrapperProps = {
    initialState?: DataStore;
};
export declare const ContextWrapper: FC<ContextWrapperProps>;
export {};
