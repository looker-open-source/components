import React from 'react'
import { Paragraph, PopoverContent } from '@looker/components'

export const popoverContent = (
  <PopoverContent>
    <Paragraph width={300} p="xxlarge">
      👋 Hello, I am a popover!
    </Paragraph>
  </PopoverContent>
)
