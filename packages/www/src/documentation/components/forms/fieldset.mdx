---
name: FieldSet
route: components/forms/fieldset
---

import { Playground } from 'docz'
import { Fieldset, FieldText, Button } from '@looker/components'
import { ComponentDetail } from '../../../components'

# FieldSet

<ComponentDetail of="Fieldset" figmaURL="file/2MG6DoSjk4IaLnjjFCnKFf/Forms" />

The `<Fieldset />` component is used to wrap Field components into meaningful groups. The `legend` property allows for a heading which is by default aligned above and to the left.

<Playground>
  <React.Fragment>
    <Fieldset legend="Legend">
      <FieldText label="First" />
      <FieldText label="Second" />
      <FieldText label="Third" />
    </Fieldset>
    <Button>Submit</Button>
  </React.Fragment>
</Playground>
