---
title: DateTime
propsOf: DateTime
---

## DateTime

The `DateTime` component will return a string with a language sensitive representation of the date. The default value will be assumed according to the default locale.

```jsx
<DateTime />
```

## DateTime Options
