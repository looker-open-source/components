---
title: Tree
github: Tree
propsOf: Tree
---

import { Props } from '../../../Shared'

`<Tree>`, like `<Accordion>`, is a collapsible section component designed for hiding and showing content.

However, unlike `<Accordion>`, `<Tree>` has a more structured interface and is best used when building tree views like a file structure or a sidebar.
