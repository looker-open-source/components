import { FieldDate } from '@looker/components';
import { Editor, excludedProps } from '@looker/storybook';
import { ArgsTable, Meta } from '@storybook/addon-docs';
import storyBasic from './Basic?raw';

<Meta component={FieldDate} title="Docs/FieldDate" />

# FieldDate

This component is composed of an `InputDate` and a `Label`.

<Editor code={storyBasic} />

## Props

<ArgsTable of={FieldDate} exclude={excludedProps} />
