import { FieldSelectMulti } from '@looker/components';
import { Editor, excludedProps } from '@looker/storybook';
import { ArgsTable, Meta } from '@storybook/addon-docs';
import storyBasic from './Basic?raw';
import storyDetail from './Detail?raw';
import storyDescription from './Description?raw';
import storyValidation from './Validation?raw';
import storyRequired from './Required?raw';
import storyDisabled from './Disabled?raw';

<Meta component={FieldSelectMulti} title="Docs/FieldSelectMulti" />

# FieldSelectMulti

The `<FieldSelectMulti />` component is composed of an `<SelectMulti />` component and a `<Label />` component.

<Editor code={storyBasic} />

### FieldSelectMulti attributes

`<FieldSelectMulti />` have attributes to help display additional information. `detail`, `description` and `validation messages` are available to the component.

<Editor code={storyDetail} />
<Editor code={storyDescription} />
<Editor code={storyValidation} />

### FieldSelectMulti props

`<FieldSelectMulti />` can be set as `disabled` and also as `required`. Aslo the width of the component will be 100% based of the parent and sibling components. Using `Grid` can be helpful to position the components.

<Editor code={storyDisabled} />
<Editor code={storyRequired} />

## Props

<ArgsTable of={FieldSelectMulti} exclude={excludedProps} />
