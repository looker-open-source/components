import { FieldDateRange } from '@looker/components'

<Meta
  component={FieldDateRange}
  title="Docs/Components/Fields/FieldDateRange"
/>

# FieldDateRange

This component is composed of an `InputDateRange` and a `Label`.

```jsx
<Grid columns={1}>
  <FieldDateRange label="Pick a date" />
  <FieldDateRange
    label="Validation Error"
    validationMessage={{
      message: 'Select a valid calendar date',
      type: 'error',
    }}
  />
  <FieldDateRange
    disabled
    label="Disabled Field"
    value={{
      from: new Date('May 18, 2020'),
      to: new Date('May 21, 2020'),
    }}
  />
</Grid>
```
