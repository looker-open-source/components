import { FieldDateRange } from '@looker/components'
import { Editor, excludedProps } from '@looker/storybook'
import { ArgsTable, Meta } from '@storybook/addon-docs'
import storyBasic from '!!raw-loader!./Basic'

<Meta component={FieldDateRange} title="Docs/FieldDateRange" />

# FieldDateRange

This component is composed of an `InputDateRange` and a `Label`.

<Editor code={storyBasic} />

## Props

<ArgsTable of={FieldDateRange} exclude={excludedProps} />
