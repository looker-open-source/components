import { InputColor } from '@looker/components';
import { Editor, excludedProps } from '@looker/storybook';
import { ArgsTable, Meta } from '@storybook/addon-docs';
import storyBasic from './Basic?raw';
import storyDefaultValue from './DefaultValue?raw';
import storyControlledColor from './ControlledColor?raw';
import storyDisabled from './Disabled?raw';
import storyDisabledNoValue from './DisabledNoValue?raw';
import storyReadOnly from './ReadOnly?raw';

<Meta component={InputColor} title="Docs/InputColor" />

# InputColor

The `InputColor` component has an input to add a color value and a color wheel to pick color directly.

<Editor code={storyBasic} />

## Value

Use the `value` prop for a controlled component.

<Editor code={storyControlledColor} />

## DefaultValue

To set an initial value on an uncontrolled component use `defaultValue`.

<Editor code={storyDefaultValue} />

## Disabled

<Editor code={storyDisabled} />
<Editor code={storyDisabledNoValue} />

## ReadOnly

<Editor code={storyReadOnly} />

## Props

<ArgsTable of={InputColor} exclue={excludedProps} />
