import { InputFile } from '@looker/components'
import { Editor, excludedProps } from '@looker/storybook'
import { ArgsTable, Meta } from '@storybook/addon-docs'
import basicStory from '!!raw-loader!./Basic'
import acceptStory from '!!raw-loader!./Accept'
import buttonTextStory from '!!raw-loader!./ButtonText'
import placeholderStory from '!!raw-loader!./Placeholder'

<Meta component={InputFile} title="Docs/InputFile" />

# InputFile

Render a file input form element.

## HandleFile

A function that takes the uploaded file

<Editor code={basicStory} />

## Accept

A string containing one or more unique file type specifiers i.e. '.pdf', each file type separated by a comma.

<Editor code={acceptStory} />

## ButtonText

Text for the button which uploads the file.

<Editor code={buttonTextStory} />

## Placeholder

Placeholder text within the text input box.

<Editor code={placeholderStory} />

<ArgsTable of={InputFile} exclude={excludedProps} />
