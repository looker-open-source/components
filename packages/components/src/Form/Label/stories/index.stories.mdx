import { Label } from '@looker/components';
import { Editor, excludedProps } from '@looker/storybook';
import { ArgsTable, Meta } from '@storybook/addon-docs';
import basicStory from './Basic?raw';
import typographyStory from './Typography?raw';

<Meta component={Label} title="Docs/Label" />

# Label

<Editor code={basicStory} />

## Typography Props

Label supports standard Typography customizations, such as `color`, `fontSize`, and `fontWeight`.

<Editor code={typographyStory} />

## Props

<ArgsTable of={Label} exclude={excludedProps} />
