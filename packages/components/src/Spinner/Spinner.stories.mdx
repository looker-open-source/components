import { Spinner } from '@looker/components'
import { Meta } from '@storybook/addon-docs'

<Meta component={Spinner} title="Docs/Components/Content/Spinner" />

# Spinner

```jsx
<Spinner />
<Spinner size={80} markers={20} markerRadius={50} color="salmon" speed={2000} />
```