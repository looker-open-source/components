import { Spinner } from '@looker/components';
import { Editor, excludedProps } from '@looker/storybook';
import { ArgsTable, Meta } from '@storybook/addon-docs';
import storyDefault from './Default?raw';
import storyColor from './Color?raw';
import storySize from './Size?raw';
import storySpeed from './Speed?raw';
import storyMarkers from './Markers?raw';

<Meta component={Spinner} title="Docs/Spinner" />

# Spinner Default

<Editor code={storyDefault} />

## Set color

<Editor code={storyColor} />

## Set size

<Editor code={storySize} />

## Set speed

<Editor code={storySpeed} />

## Set markers

<Editor code={storyMarkers} />

<ArgsTable of={Spinner} exclude={excludedProps} />
