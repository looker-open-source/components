import { ButtonOutline, Code, MessageBar } from '@looker/components'
import { Editor, excludedProps } from '@looker/storybook'
import { ArgsTable, Meta } from '@storybook/addon-docs'
import storyBasic from '!!raw-loader!./Basic'
import storyCritical from '!!raw-loader!./Critical'
import storyLarge from '!!raw-loader!./Large'
import storyMedium from '!!raw-loader!./Medium'
import storyNeutral from '!!raw-loader!./Neutral'
import storySmall from '!!raw-loader!./Small'
import storyXSmall from '!!raw-loader!./XSmall'

<Meta component={ButtonOutline} title="Docs/ButtonOutline" />

# ButtonOutline

Use an outline button alongside a default button to provide alternative actions on a page. Outline buttons should only appear alongside default buttons for secondary actions. Use no more than two secondary buttons on a page.

<Editor code={storyBasic} />

## Color

`Key` is the primary brand color, which is the default setting.
`Critical` Buttons are to be used in situations where you need to convey some very important, potentially irreversible consequence of pressing this button.

<Editor code={storyCritical} />

`Neutral` buttons are meant to fade into the background. These are usually used for secondary, nonessential, actions.

<Editor code={storyNeutral} />

## Size

Use the size property on a `<ButtonOutline />` to modify the size the button rendered. You can combine it with the `mode` property to get the correct style and size of button you need.

<Editor code={storyXSmall} />
<Editor code={storySmall} />
<Editor code={storyMedium} />
<Editor code={storyLarge} />

## Props

<ArgsTable of={ButtonOutline} exclude={excludedProps} />
