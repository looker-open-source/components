import { MultiFunctionButton } from '@looker/components'
import { Editor, excludedProps } from '@looker/storybook'
import { ArgsTable, Meta } from '@storybook/addon-docs'
import storyCopyToClipboard from '!!raw-loader!./CopyToClipboard'

<Meta component={MultiFunctionButton} title="Docs/MultiFunctionButton" />

# MultiFunctionButton

Here's an example of implementing a multi function button, like conveying to a user that the content has been copied to the clipboard:

<Editor code={storyCopyToClipboard} />

## Props

<ArgsTable of={MultiFunctionButton} exclude={excludedProps} />
