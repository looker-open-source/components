import { ChipButton } from '@looker/components';
import { Editor, excludedProps } from '@looker/storybook';
import { ArgsTable, Meta } from '@storybook/addon-docs';
import storyBasic from './Basic?raw';
import storyDeletable from './Deletable?raw';
import storyDeSelected from './DeSelected?raw';
import storyDisabled from './Disabled?raw';
import storyMaxWidth from './MaxWidth?raw';
import storyPrefix from './Prefix?raw';

<Meta component={ChipButton} title="Docs/ChipButton" />

# ChipButton

The `ChipButton` component is a compact button element.

## Basic

It has some basic functionality

<Editor code={storyBasic} />

## Deletable

It can have `onDelete` available

<Editor code={storyDeletable} />

## Disabled

It can be disabled.

<Editor code={storyDisabled} />

## MaxWidth

The width of the `ChipButton` will accomodate its contents, by default. Setting a `maxWidth` will cause any overflow text to be truncated with ellipses.

<Editor code={storyMaxWidth} />

## Prefix

Optional string parameter that will be added before a `ChipButton`'s contents and separated by a colon.

<Editor code={storyPrefix} />

## Props

<ArgsTable of={ChipButton} exclude={excludedProps} />
