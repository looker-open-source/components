import { Breakpoint } from '@looker/components'
import { Editor, excludedProps } from '@looker/storybook'
import { ArgsTable, Meta } from '@storybook/addon-docs'
import storyMobileLaptop from '!!raw-loader!./MobileLaptop'
import storyDesktop from '!!raw-loader!./Desktop'
import storyDesktopXL from '!!raw-loader!./DesktopXL'
import storyXL from '!!raw-loader!./XL'

<Meta component={Breakpoint} title="Docs/Breakpoint" />

# Breakpoint

The `Breakpoint` component is used to render content at desired screen sizes.

While we recommend using css breakpoints or styled-system when possible, this component is provided to facilitate larger markup or behavior changes tailored to the device.

We support five named breakpoints as follows:

| Name    | Width (REM)   | Width (PX)      |
| ------- | ------------- | --------------- |
| mobile  | 1rem – 30rem  | 1px – 480px     |
| tablet  | 31rem – 48rem | 481px – 768px   |
| laptop  | 49rem – 64rem | 769px – 1024px  |
| desktop | 65rem – 75rem | 1025px – 1200px |
| xl      | >= 76rem      | >= 1201px       |

## Rendering content on a range of devices

Use the `show` prop to specify either a string name of a single device (e.g. `'mobile'`), or a 2-length tuple to define a range of devices (`['mobile', 'desktop']` &ndash; "render from mobile to desktop").

<Editor code={storyMobileLaptop} />
<Editor code={storyDesktop} />
<Editor code={storyDesktopXL} />
<Editor code={storyXL} />

## Props

<ArgsTable of={Breakpoint} exclude={excludedProps} />
