import { Divider } from '@looker/components'
import { Editor, excludedProps } from '@looker/storybook'
import { ArgsTable, Meta } from '@storybook/addon-docs'
import storyAppearance from '!!raw-loader!./Appearance'
import storyBasic from '!!raw-loader!./Basic'
import storyCustom from '!!raw-loader!./Custom'

<Meta component={Divider} title="Docs/Divider" />

# Divider

Dividers accept all the spacing props from `<Box />`, which gives you the ability to easily put spacing around your dividers. By default a divider's background color will be `charcoal300`

<Editor code={storyBasic} />

## Appearance

There are three divider appearances `light`, `dark` and `onDark`.

<Editor code={storyAppearance} />

## Custom

This example shows a size of 20px and turquoise custom color.

<Editor code={storyCustom} />

## Props

<ArgsTable of={Divider} exclude={excludedProps} />
