import { theme } from '@looker/design-tokens'
import { RadiusList } from './demos/RadiusList'
const radiiCount = Object.entries(theme.radii).length

<Meta title="Foundations/Radius" />

# Radius

<p>
  Radius values are used to apply border radius to components. They use our
  t-shirt size naming pattern with {radiiCount} levels of radii.
</p>

<RadiusList />
