import type { SDKRecord, Fields, Pivots } from '../types';
export declare const mockPivotedQuery: {
    data: SDKRecord[];
    fields: Fields;
    pivots: Pivots;
    totals: Record<string, number>;
};
