import type { CScatter } from '../adapters';
import type { ConfigHelper } from '../types';
/**
 * Populate series size_by attribute from the size_by_field response
 * Works with both named series and array series options.
 */
export declare const seriesSize: ConfigHelper<CScatter>;
