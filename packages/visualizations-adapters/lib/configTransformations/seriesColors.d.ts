import type { ConfigHelper, CommonCartesianProperties } from '../types';
/**
 * Populate series colors from series_colors response.
 * Can merge default values with either array or named object
 */
export declare const seriesColors: ConfigHelper<CommonCartesianProperties>;
