import type { ConfigHelper, CommonCartesianProperties } from '../types';
/**
 * Combine `legend_position` and `hide_legend` into a single `legend` object.
 */
export declare const legendPosition: ConfigHelper<CommonCartesianProperties>;
