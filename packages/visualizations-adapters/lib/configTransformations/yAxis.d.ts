import type { ConfigHelper, CCartesian } from '../types';
/**
 * Combine `y_axis_reversed` and `y_axes: {showValues, showLabels}` into
 * `y_axis: {reversed, show_values, show_label }`
 */
export declare const yAxis: ConfigHelper<CCartesian>;
