import { TreeSelect } from '@looker/filter-components'
import { ImportMessage, Editor, excludedProps } from '@looker/storybook'
import { ArgsTable, Meta } from '@storybook/addon-docs'
import storyWithoutDropdown from '!!raw-loader!./WithoutDropdown'
import { exploreData } from './examples'

<Meta component={TreeSelect} title="Filters/Docs/TreeSelect" />

# TreeSelect

<ImportMessage packageName="filter-components">TreeSelect</ImportMessage>

`TreeSelect` renders a tree of explore fields for selection.

<Editor code={storyWithoutDropdown} scope={{ exploreData }} />

## Props

<ArgsTable of={TreeSelect} exclude={excludedProps} />
